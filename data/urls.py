HOST = "https://www.qa-practice.com/"

class Urls:
    class Elements:

        class Buttons:
            SIMPLE = f"{HOST}elements/button/simple"
            DISABLED = f"{HOST}elements/button/disabled"
            LIKE_A_BUTTON = f"{HOST}elements/button/like_a_button"

        class Inputs:
            SIMPLE = f"{HOST}elements/input/simple"
            EMAIL = f"{HOST}elements/input/email"
            PASSWORD = f"{HOST}elements/input/passwd"

    # BASE_URL = "https://www.qa-practice.com/"
    #
    # SIMPLE_INPUT = f"{BASE_URL}elements/input/simple"
    # EMAIL_INPUT = f"{BASE_URL}elements/input/email"
    # PASSWORD_INPUT = f"{BASE_URL}elements/input/passwd"
    #
    # # Buttons
    # SIMPLE_BUTTON = f"{BASE_URL}elements/button/simple"
    # LIKE_A_BUTTON = f"{BASE_URL}elements/button/like_a_button"
    # DISABLE_BUTTON = f"{BASE_URL}elements/button/disabled"